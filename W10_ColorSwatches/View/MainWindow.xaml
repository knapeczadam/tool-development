<Window x:Class="W10_ColorSwatches.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:W10_ColorSwatches"
        xmlns:viewModel="clr-namespace:W10_ColorSwatches.ViewModel"
        xmlns:view="clr-namespace:W10_ColorSwatches.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>
    
    <Window.InputBindings>
        <KeyBinding Command="{Binding UndoCommand}" Key="Z" Modifiers="Ctrl" />
        <KeyBinding Command="{Binding RedoCommand}" Key="Y" Modifiers="Ctrl" />
    </Window.InputBindings>
    
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Command="{Binding UndoCommand}"/>
                <MenuItem Header="_Redo" Command="{Binding RedoCommand}"/>
            </MenuItem>
        </Menu>
        <Grid>
            <!-- Main layout-->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!-- Add / Remove buttons -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding AddSwatchCommand}">+</Button>
                <Button Grid.Column="1" Command="{Binding DeleteSwatchCommand}" CommandParameter="{Binding SelectedSwatch}">-</Button>
            </Grid>

            <!-- Swatches List -->
            <ListView Grid.Row="1" Grid.Column="0" 
                  ItemsSource="{Binding Swatches}" 
                  SelectedItem="{Binding SelectedSwatch}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="20" Height="20" Fill="{Binding Color, Converter={StaticResource ColorToBrush}}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Swatch Control-->
            <view:SwatchControl Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" DataContext="{Binding SelectedSwatch}"/>
        </Grid>
    </DockPanel>
</Window>
