<UserControl x:Class="W10_ApexIcons.Views.IconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:W10_ApexIcons.Views"
             xmlns:dataProviders="clr-namespace:W10_ApexIcons.DataProviders"
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="200" d:DesignWidth="200">
    <Grid DataContext="{Binding ElementName=Root, Path=IconSource}"
          d:DataContext="{Binding Source={x:Static dataProviders:DesignTimeData.MockIconData  }}">
        
        <Border>
            <Rectangle Fill="{Binding Color, Converter={StaticResource ColorToBrush}}">
                <Rectangle.OpacityMask>
                    <ImageBrush ImageSource="{Binding SkillType, Converter={StaticResource SkillToImage}}"/>
                </Rectangle.OpacityMask>
            </Rectangle>
            
        </Border>
    </Grid>
</UserControl>
