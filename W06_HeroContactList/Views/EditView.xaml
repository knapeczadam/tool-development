<Window x:Class="W06_HeroContactList.Views.EditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:W06_HeroContactList.Views"
        mc:Ignorable="d"
        Title="Edit Contact" Height="130" Width="270" 
        DataContext="{Binding Mode=OneWay, Source={StaticResource editVM}}">
    <Grid>
        <Image x:Name="image" HorizontalAlignment="Left" Height="79" Margin="10,10,0,0" VerticalAlignment="Top" Width="83" Source="{Binding TargetContact.Avatar}"/>
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Margin="109,10,0,0" TextWrapping="Wrap" Text="{Binding NewFirstName}" VerticalAlignment="Top" Width="151"/>
        <TextBox x:Name="textBox1" HorizontalAlignment="Left" Margin="109,0,0,0" TextWrapping="Wrap" Text="{Binding NewLastName}" VerticalAlignment="Center" Width="151"/>
        <Button x:Name="button" 
                Content="OK" 
                HorizontalAlignment="Left" 
                Margin="194,84,0,0" 
                VerticalAlignment="Top" Width="66" 
                Command="{Binding ApplyChangesCommand, Mode=Default}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

    </Grid>
</Window>
